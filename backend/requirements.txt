# ── Web API ──
fastapi==0.135.3
uvicorn==0.42.0
pydantic==2.12.5
python-dotenv>=1.0.0
python-multipart==0.0.22

# ── AI (Groq LLM — no local model needed) ──
groq>=1.2.0

# ── Embeddings (HF API in prod; local fallback in dev) ──
# sentence-transformers and torch are optional — only needed locally.
# On Render, set HF_API_TOKEN and these are NOT installed.
numpy==2.4.4

# ── Database ──
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.9

# ── Utilities & Graphs ──
requests==2.33.1
psutil>=5.9.0
networkx==3.6.1
graphviz==0.21
